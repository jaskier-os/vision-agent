FROM node:20-alpine

WORKDIR /app

# Vendored orchestrator SDK (point-in-time copy; see README)
COPY sdk/ sdk/

COPY package.json ./

RUN npm install --omit=dev

COPY src/ src/

CMD ["node", "src/agent-entry.js"]
